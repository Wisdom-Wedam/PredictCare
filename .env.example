# Gemini API key for intelligent symptom extraction & explanations (server-side only)
GEMINI_API_KEY=

# MongoDB connection (optional - falls back to local JSON store if offline)
MONGODB_URI=
MONGODB_DB_NAME=predictcare
